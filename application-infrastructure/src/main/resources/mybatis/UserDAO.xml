<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lecture.infr.dao.UserDAO">
    <insert id="register" useGeneratedKeys="true" keyColumn="uid" keyProperty="userDO.uid">
        INSERT
        INTO userAggregate(account, password, type, phone, email)
        VALUE (#{account}, #{password}, #{type}, #{phone}, #{email})
    </insert>
</mapper>